import os
import sys

import uvicorn

# 确保 app 模块可导入
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from app import create_app

# 主应用
app = create_app()
# 如需API版本管理:
# v1 = create_app(prefix="/v1")
# app.mount("/v1", v1)

if __name__ == "__main__":
    uvicorn.run("main:app", host="0.0.0.0", port=8201, reload=True)
