<view class="page-container dark">
  <!-- 光晕背景层 -->
  <view class="orb-bg"></view>

  <!-- 加载状态 -->
  <loading visible="{{loading}}" text="加载中..." />

  <!-- 页面内容 -->
  <scroll-view scroll-y class="page-scroll" wx:if="{{!loading}}">

    <!-- 升级提示条 -->
    <view class="upgrade-hint" wx:if="{{showUpgradeHint}}" bindtap="goUpgrade">
      <view class="hint-content">
        <text class="hint-icon">⭐</text>
        <text class="hint-text">{{upgradeHintText}}</text>
        <text class="hint-action">去升级 ›</text>
      </view>
      <view class="hint-close" catchtap="closeUpgradeHint">
        <text>✕</text>
      </view>
    </view>

    <!-- 用户信息头部 -->
    <view class="user-header glass-layer-1">
      <view class="user-avatar">
        <image src="{{userInfo.avatar || '/images/default-avatar.png'}}" mode="aspectFill" />
        <view class="member-badge badge-{{memberLevel}}">{{memberLevelText}}</view>
      </view>
      <view class="user-meta">
        <view class="user-name">{{userInfo.name || '未设置姓名'}}</view>
        <view class="user-position">{{userInfo.company}} {{userInfo.title}}</view>
      </view>
      <view class="user-stats">
        <view class="stat-item">
          <text class="stat-num">{{stats.visitors || 0}}</text>
          <text class="stat-label">访客</text>
        </view>
        <view class="stat-item">
          <text class="stat-num">{{stats.matches || 0}}</text>
          <text class="stat-label">匹配</text>
        </view>
        <view class="stat-item">
          <text class="stat-num">{{stats.trust || 0}}</text>
          <text class="stat-label">信任</text>
        </view>
      </view>
    </view>

    <!-- 快速操作栏 -->
    <view class="action-bar glass-layer-2">
      <view class="action-item" bindtap="goEditCard">
        <text class="action-icon">✏️</text>
        <text class="action-label">编辑名片</text>
      </view>
      <view class="action-item" bindtap="goPreview">
        <text class="action-icon">👁️</text>
        <text class="action-label">预览画册</text>
      </view>
      <view class="action-item" bindtap="goQrCode">
        <text class="action-icon">📱</text>
        <text class="action-label">我的二维码</text>
      </view>
      <view class="action-item" bindtap="shareCard">
        <text class="action-icon">📤</text>
        <text class="action-label">分享名片</text>
      </view>
    </view>

    <!-- AI能力中心入口 -->
    <view class="card ai-entry-card" bindtap="goAiCenter">
      <view class="ai-entry-bg">
        <view class="ai-entry-content">
          <view class="ai-entry-icon-wrap">
            <text class="ai-entry-icon">🤖</text>
          </view>
          <view class="ai-entry-text">
            <text class="ai-entry-title">AI能力中心</text>
            <text class="ai-entry-desc">智能对话 · 名片扫描 · 人脉匹配 · 数据洞察</text>
          </view>
          <text class="ai-entry-arrow">›</text>
        </view>
        <view class="ai-entry-glow"></view>
      </view>
    </view>

    <!-- 场景模式切换 - P2功能 -->
    <scene-selector 
      wx:if="{{showSceneSelector}}"
      bind:scenechange="onSceneChange"
      current="{{currentScene}}" />

    <!-- 我的名片卡片 -->
    <view class="card brochure-card glass-layer-2" bindtap="goPreview">
      <view class="card-header">
        <text class="title">📖 我的画册</text>
        <text class="more">查看全部 ›</text>
      </view>
      <view class="brochure-cover">
        <image src="{{brochure.cover || '/images/default-cover.png'}}" mode="aspectFill" />
        <view class="brochure-info">
          <text class="brochure-name">{{brochure.title || '我的AI数智名片'}}</text>
          <text class="brochure-stats">{{brochure.viewCount || 0}}次浏览 · {{brochure.pageCount || 0}}页</text>
        </view>
      </view>
    </view>

    <!-- 创建资源平台入口 -->
    <view class="card platform-entry" bindtap="goCreatePlatform">
      <view class="platform-entry-bg">
        <view class="platform-entry-content">
          <text class="platform-entry-icon">🏗️</text>
          <view class="platform-entry-text">
            <text class="platform-entry-title">创建资源平台</text>
            <text class="platform-entry-desc">创建专属平台，整合优质资源</text>
          </view>
          <text class="platform-entry-arrow">›</text>
        </view>
      </view>
    </view>

    <!-- 信任网络 → 图谱入口 -->
    <view class="card glass-layer-2" bindtap="goTrust" style="cursor:pointer;">
      <view class="card-header">
        <text class="title">🤝 信任网络</text>
        <text class="more">图谱模式 ›</text>
      </view>
      <view class="trust-summary">
        <text class="trust-count">{{trustCount}} 位信任伙伴</text>
        <view class="trust-avatars" wx:if="{{trustList.length > 0}}">
          <image class="trust-avatar" wx:for="{{trustList.slice(0, 5)}}" wx:key="id"
            src="{{item.avatar || '/images/default-avatar.png'}}" mode="aspectFill" />
          <view class="trust-more" wx:if="{{trustCount > 5}}">+{{trustCount - 5}}</view>
        </view>
      </view>
    </view>

    <!-- 匹配推荐区 -->
    <view class="card glass-layer-2" wx:if="{{recommendList.length > 0}}">
      <view class="card-header">
        <text class="title">🎯 推荐匹配</text>
        <text class="more" bindtap="goMatch">查看更多 ›</text>
      </view>
      <view class="recommend-list">
        <view class="recommend-item" wx:for="{{recommendList.slice(0, 3)}}" wx:key="id"
          data-id="{{item.id}}" bindtap="goMatchDetail">
          <image src="{{item.avatar || '/images/default-avatar.png'}}" mode="aspectFill" class="rec-avatar" />
          <view class="rec-info">
            <text class="rec-name">{{item.name}}</text>
            <text class="rec-company">{{item.company}}</text>
            <view class="rec-tags" wx:if="{{item.commonTags}}">
              <text class="rec-tag" wx:for="{{item.commonTags.slice(0, 2)}}" wx:key="index">{{item}}</text>
            </view>
          </view>
          <view class="rec-score">
            <text class="score-num">{{item.matchScore}}%</text>
            <text class="score-lbl">匹配</text>
          </view>
        </view>
      </view>
    </view>

    <!-- 空白状态 -->
    <empty-state visible="{{!loading && !brochure && recommendList.length === 0}}" text="暂无数据，快去创建你的名片吧" />

    <!-- 底部占位 -->
    <view class="bottom-placeholder"></view>
  </scroll-view>
</view>
