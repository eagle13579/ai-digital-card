# AI数智名片 — 基线测试清单

> 生成时间: 2026-07-26
> 基准版本: 基于已完成修复的代码（路径修复、登录流程修复、baseURL统一）
> 环境: 微信开发者工具 + 生产域名 https://card.liankebao.top

---

## 一、全局检查（每次改动后必做）

| # | 检查项 | 方法 | 预期结果 |
|---|--------|------|----------|
| 1 | 编译无报错 | 微信DevTools → 编译 | 控制台无红色 error 输出 |
| 2 | 无 module not found | `grep -rn "require.*\.\./\.\./utils" pages/*/index.js` | 应无输出（所有 require 路径正确） |
| 3 | 无 require 路径回退 | `grep -rn "require.*\.\./\.\./utils" pages/*/*.js` | 应无输出 |
| 4 | 桥接文件引用正确 | `grep -rn "require.*mockService" utils/*-bridge.js` | 全部使用 `'./mockService'`（非 `'../../utils/mockService'`） |
| 5 | app.json pages 完整性 | 对比 `app.json` 中 `pages` 数组与物理文件夹 | 注册的页面路径都能找到对应的 `.js/.wxml/.wxss/.json` 文件 |
| 6 | 无强制跳登录残留 | `grep -n "reLaunch.*login\|redirectTo.*login" app.js | head -5` | 应无输出（100ms强制跳转已删除） |
| 7 | API_BASE_URL 统一 | `grep -rn "192\.168\." miniapp/ --include="*.js"` | 应无输出（所有地址已统一为 `card.liankebao.top`） |
| 8 | wx.request 超时设置 | `grep -rn "timeout" utils/request.js` | timeout=8000ms |
| 9 | 离线缓存备用 | 断网后打开已访问过的页面 | 应有 Toast "网络异常，已加载离线缓存" |
| 10 | mock 回退正常 | `getApp().globalData.useRealApi` 设为 false 后重启 | 所有列表页面展示 mock 数据，无崩溃 |
| 11 | 控制台无 deprecated 警告 | 微信DevTools 编译后查看 console | 无黄色 deprecation 警告（或仅已知可忽略项） |

---

## 二、登录流程测试

| 步骤 | 操作 | 预期 | 检查点 |
|------|------|------|--------|
| 1 | 清除所有 storage → 打开小程序 | 显示登录页 | 当前页为 `pages/login/index` |
| 2 | 点击「一键登录」按钮 | ① `wx.login()` 成功获取 code → ② 调 `POST /api/auth/wx-mini-login` → ③ 返回 200 + token → ④ 跳转首页 | token 存到 `wx.setStorageSync('token')` |
| 3 | 点击「暂不登录」按钮 | ① `skipLogin()` 生成 mock token → ② `store.login()` → ③ `wx.redirectTo` 首页 | `store.getState().isLoggedIn === true`，首页正常渲染 mock 数据 |
| 4 | 有 token 时关闭重开 | 跳过登录页，直接显示首页 | entryPagePath 被重定向，首页 onLoad 触发 |
| 5 | 过期 token 场景 | 手动修改 storage token 为过期值 → 重启 | `request.js` 收到 401 → 自动清除 token → 跳转登录页 |
| 6 | mock token 过期 | 使用 mock token → 后台 API 返回 401 | 同上，自动清除 mock token 并跳登录页 |
| 7 | 登录后手动登出 | 在「我的」页点击退出登录 | token 清除 → 跳回登录页 |

---

## 三、每个页面的冒烟测试

### 3.1 首页（Tab: 首页）
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从登录页正常跳转或切换 Tab | `pages/index/index` onLoad 不崩溃，展示名片列表/推荐 |
| 2 | 下拉刷新 | 首页下拉 | `onPullDownRefresh` 触发，列表刷新 |
| 3 | 名片点击 | 点击任意名片卡片 | 跳转名片详情（`pages/brochure/preview/index`） |

### 3.2 名片页（Tab: 名片）
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 切换至「名片」Tab | `pages/card/card` onLoad 不崩溃，展示我的电子名片 |
| 2 | 名片展示 | 查看名片信息 | 头像、姓名、公司、职位等字段正常渲染 |
| 3 | 分享名片 | 点击分享按钮 | 调起微信分享面板 |

### 3.3 我的（Tab: 我的）
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 切换至「我的」Tab | `pages/profile/profile` onLoad 不崩溃，展示个人中心 |
| 2 | 个人信息展示 | 查看页面内容 | 头像、昵称、会员等级等基本信息展示 |
| 3 | 退出登录 | 点击退出登录按钮 | 弹出确认 → token 清除 → 跳转登录页 |

### 3.4 登录页
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 清除 storage 后启动 | 登录页正常渲染，无崩溃 |
| 2 | 用户协议/隐私链接 | 点击协议链接 | 跳转 `pages/agreement/user` 或 `pages/agreement/privacy/index` |

### 3.5 搜索
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从首页进入搜索 | `pages/search/index` onLoad 不崩溃 |
| 2 | 关键词搜索 | 输入关键词 → 搜索 | 调 `GET /api/users/search/list?keyword=xxx`，展示结果列表 |
| 3 | 搜索结果点击 | 点击某条搜索结果 | 进入对应用户详情或名片预览 |

### 3.6 创建名片
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从首页点击「新建名片」 | `pages/brochure/create/index` onLoad 不崩溃，4步引导展示 |
| 2 | 表单填写 | 填写基本信息、专业信息、公司信息 | 各步骤表单组件正常，无渲染异常 |
| 3 | 预览发布 | 完成所有步骤 → 点击发布 | 调 `POST /api/brochures` → 成功后跳转预览页 |

### 3.7 预览名片
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从创建页跳转或直接进入 | `pages/brochure/preview/index` onLoad 不崩溃，翻页画册展示 |
| 2 | 翻页交互 | 左右滑动/点击翻页 | `flip-book` 组件正常工作，无白屏/卡顿 |
| 3 | 分享 | 点击分享按钮 | 调 `miniappApi.share()`，生成分享图 |

### 3.8 创建平台
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从平台列表进入创建 | `pages/platform/create/index` onLoad 不崩溃 |
| 2 | 省市选择 | 点击省市选择器 | 省/市/区级联选择弹窗正常 |
| 3 | 表单提交 | 填写信息 → 提交 | 调 `platform-bridge` → 无报错 |

### 3.9 平台详情
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从平台列表点击进入 | `pages/platform/detail/index` onLoad 不崩溃，显示平台详细资料 |
| 2 | 成员列表 | 滚动查看成员列表 | 成员列表正常渲染，无空白 |
| 3 | 加入平台 | 点击加入按钮 | 调 `POST /api/business-card/platforms/{id}/join` |

### 3.10 平台管理
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从平台详情进入管理 | `pages/platform/manage/index` onLoad 不崩溃 |
| 2 | 成员审核 | 审核/拒绝待审核成员 | 调 `platform-bridge` 对应接口，无报错 |
| 3 | 平台设置修改 | 修改平台信息 → 保存 | 调 `PUT /api/business-card/platforms/{id}` |

### 3.11 平台列表
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从首页进入平台列表 | `pages/platform/list/index` onLoad 不崩溃，展示平台列表 |
| 2 | 搜索/筛选 | 输入关键词筛选平台 | 调 `GET /api/business-card/platforms?keyword=xxx` |
| 3 | 平台点击 | 点击平台条目 | 跳转 `pages/platform/detail/index` |

### 3.12 二维码
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从名片页生成二维码 | `pages/qrcode/index` onLoad 不崩溃 |
| 2 | 二维码展示 | 查看二维码图片 | 二维码正确生成，可被扫码识别 |
| 3 | 保存到相册 | 长按/点击保存 | 调 `wx.saveImageToPhotosAlbum` |

### 3.13 会员
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从「我的」页进入会员中心 | `pages/membership/index` onLoad 不崩溃 |
| 2 | 会员等级展示 | 查看会员级别/权益对比 | 各等级权益列表正常渲染 |
| 3 | 购买会员 | 点击购买按钮 | 调 `POST /api/payment/create`，调起微信支付 |

### 3.14 隐私政策
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从登录页或其他入口进入 | `pages/agreement/privacy/index` onLoad 不崩溃 |
| 2 | 内容展示 | 滚动查看隐私政策全文 | 富文本内容正常渲染，无白屏 |

### 3.15 用户协议
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从登录页或其他入口进入 | `pages/agreement/user` onLoad 不崩溃 |
| 2 | 内容展示 | 滚动查看协议全文 | 富文本内容正常渲染，无白屏 |

### 3.16 网络图谱
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从网络入口进入 | `pages/network/graph/index` onLoad 不崩溃 |
| 2 | 图谱渲染 | 查看信任网络可视化 | 节点/边正确绘制，`network-mini-map` 组件工作正常 |
| 3 | 节点交互 | 点击图谱中的节点 | 展示节点详情或跳转对应用户名片 |

### 3.17 导入联系人
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从任意入口进入 | `pages/network/import/index` onLoad 不崩溃 |
| 2 | 导入操作 | 选择文件或粘贴内容 → 导入 | 数据解析正常，无崩溃 |
| 3 | 导入结果 | 查看导入结果 | 成功/失败清单展示正确 |

### 3.18 AI匹配
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从AI首页进入 | `pages/ai/match/index` onLoad 不崩溃 |
| 2 | 匹配推荐 | 查看推荐列表 | 调 `GET /api/match/recommend`，列表正常渲染 |
| 3 | 解锁匹配 | 点击解锁匹配对象 | 调 `POST /api/match/{id}/unlock` |

### 3.19 AI洞察
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从AI首页进入 | `pages/ai/insight/index` onLoad 不崩溃 |
| 2 | 数据分析展示 | 查看数据可视化 | 统计图表/洞察列表正常渲染 |
| 3 | 数据筛选 | 切换时间范围或筛选条件 | 数据刷新，无渲染异常 |

### 3.20 AI扫描（OCR）
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从AI首页进入 | `pages/ai/scan/index` onLoad 不崩溃 |
| 2 | 拍照扫描 | 拍照 → 识别 | 调 `POST /api/ocr/scan`（上传图片），返回结构化名片信息 |
| 3 | 相册选择 | 从相册选取名片图片 | 同上，图片上传到 OCR 接口正常 |

### 3.21 AI对话
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从AI首页进入对话 | `pages/ai/chat/index` onLoad 不崩溃 |
| 2 | 发送消息 | 输入文字 → 发送 | 调 `POST /api/ai/chat`，获取AI回复并展示 |
| 3 | 对话历史 | 返回再进入 | 对话历史正常加载，可继续对话 |

### 3.22 AI生成
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从AI首页进入 | `pages/ai/generate/index` onLoad 不崩溃 |
| 2 | 内容生成 | 选择用途 → 生成 | 调 `POST /api/ai/generate`，生成内容展示 |
| 3 | 复制/保存结果 | 点击复制或保存按钮 | 内容正常复制到剪贴板或保存 |

### 3.23 AI配置
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从AI首页进入 | `pages/ai/config/index` onLoad 不崩溃 |
| 2 | 配置修改 | 修改模型参数/偏好设置 | 设置保存到 storage，无报错 |
| 3 | 配置重置 | 点击恢复默认 | 配置项恢复默认值 |

### 3.24 AI首页（入口）
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从首页进入AI模块 | `pages/ai/index/index` onLoad 不崩溃 |
| 2 | 功能入口点击 | 分别点击对话、生成、洞察、扫描、匹配等入口 | 跳转到对应子页面，无报错 |
| 3 | 数据概览 | 查看AI功能数据概览 | 统计数据正常渲染 |

### 3.25 AI盖亚（Gaia）
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从AI首页进入 | `pages/ai/gaia` onLoad 不崩溃 |
| 2 | 界面交互 | 操作盖娅大脑界面 | 所有组件正常渲染，无报错 |

### 3.26 AI A/B测试
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从AI首页进入 | `pages/ai/abtest` onLoad 不崩溃 |
| 2 | 测试列表 | 查看A/B测试列表 | 列表正常渲染，无空数据异常 |

### 3.27 AI反馈
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从AI首页进入 | `pages/ai/feedback` onLoad 不崩溃 |
| 2 | 提交反馈 | 填写反馈内容 → 提交 | 表单提交正常，无报错 |

### 3.28 AI模型服务
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从AI首页进入 | `pages/ai/modelserve` onLoad 不崩溃 |
| 2 | 状态查看 | 查看各模型服务状态 | 状态指示器正常渲染 |

### 3.29 消息列表
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从消息入口进入 | `pages/message/list/index` onLoad 不崩溃 |
| 2 | 会话列表 | 查看会话列表 | 调 `GET /api/messages`，会话列表正常渲染 |
| 3 | 会话点击 | 点击某个会话 | 跳转 `pages/message/chat/index` |

### 3.30 聊天详情
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从会话列表点击进入 | `pages/message/chat/index` onLoad 不崩溃 |
| 2 | 发送消息 | 输入文字 → 发送 | 调 `POST /api/messages`，消息气泡展示 |
| 3 | 消息历史 | 上滑加载更多 | 历史消息分页加载正常 |

### 3.31 团队列表
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从团队入口进入 | `pages/team/list/index` onLoad 不崩溃 |
| 2 | 团队列表展示 | 查看我的团队列表 | 调 `GET /api/teams`，列表正常渲染 |
| 3 | 团队点击 | 点击团队条目 → 进入详情/管理 | 跳转 `pages/team/manage/index` |

### 3.32 团队管理
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从团队列表进入 | `pages/team/manage/index` onLoad 不崩溃 |
| 2 | 成员管理 | 查看/移除成员 | 团队成员列表展示，移除功能正常 |
| 3 | 团队设置 | 修改团队名称/信息 | 调 `PUT /api/teams/{id}` |

### 3.33 组织列表
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从组织入口进入 | `pages/organization/list/index` onLoad 不崩溃 |
| 2 | 组织列表展示 | 查看组织列表 | 列表正常渲染 |
| 3 | 组织点击 | 点击组织条目 | 跳转 `pages/organization/detail/index` |

### 3.34 组织详情
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从组织列表点击进入 | `pages/organization/detail/index` onLoad 不崩溃 |
| 2 | 信息展示 | 查看组织详细资料 | 组织信息、成员等正常渲染 |
| 3 | 加入/退出 | 点击加入或退出组织 | 对应API调用正常 |

### 3.35 创建组织
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从组织列表点击创建 | `pages/organization/create/index` onLoad 不崩溃 |
| 2 | 表单提交 | 填写组织信息 → 提交 | 调 `POST /api/business-card/organizations` |
| 3 | 表单验证 | 提交空表单/必填项缺失 | 表单验证提示正常显示 |

### 3.36 建联
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从社交关系入口进入 | `pages/connections/index` onLoad 不崩溃 |
| 2 | 建联请求 | 向其他用户发送建联请求 | 调 `POST /api/business-card/connections/request` |
| 3 | 待审核列表 | 查看待审核的建联请求 | 调 `GET /api/business-card/connections/pending` |

### 3.37 分析看板
| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1 | 页面加载 | 从「我的」进入数据统计 | `pages/analytics/index/index` onLoad 不崩溃 |
| 2 | 数据展示 | 查看统计看板 | 各统计图表/卡片正常渲染 |
| 3 | 日期筛选 | 切换统计时间范围 | 数据刷新，无白屏 |

---

## 四、核心API链路测试（手工 / curl验证）

### 4.1 认证API
| # | 端点 | 方法 | 参数 | 预期结果 |
|---|------|------|------|----------|
| 1 | `/api/auth/wx-mini-login` | POST | `{ code, user_info }` | 200，返回 `{ code:0, data: { token, user } }` |
| 2 | `/api/auth/me` | GET | Header: `Authorization: Bearer <token>` | 200，返回当前用户信息 |
| 3 | `/api/auth/logout` | POST | Header: `Authorization: Bearer <token>` | 200，登出成功 |

### 4.2 名片/画册API
| # | 端点 | 方法 | 参数 | 预期结果 |
|---|------|------|------|----------|
| 1 | `/api/brochures` | GET | `?page=1&size=20` | 200，返回名片列表 |
| 2 | `/api/brochures` | POST | `{ name, company, title, ... }` | 201，返回新创建的名片 |
| 3 | `/api/brochures/{id}` | GET | — | 200，返回名片详情 |
| 4 | `/api/v1/miniapp/cards` | GET | `?page=1&size=20` | 200，返回首页卡片列表 |

### 4.3 用户API
| # | 端点 | 方法 | 参数 | 预期结果 |
|---|------|------|------|----------|
| 1 | `/api/users/me` | GET | Header: `Authorization: Bearer <token>` | 200，返回当前用户详情 |
| 2 | `/api/users/me` | PUT | `{ nickname, avatar, ... }` | 200，更新成功 |
| 3 | `/api/users/search/list` | GET | `?keyword=张三&page=1` | 200，返回搜索结果 |

### 4.4 平台API
| # | 端点 | 方法 | 参数 | 预期结果 |
|---|------|------|------|----------|
| 1 | `/api/business-card/platforms` | GET | `?keyword=&skip=0&limit=20` | 200，返回平台列表 |
| 2 | `/api/business-card/platforms/{id}` | GET | — | 200，返回平台详情 |
| 3 | `/api/business-card/platforms/{id}/join` | POST | — | 200，加入平台成功 |

### 4.5 AI模块API
| # | 端点 | 方法 | 参数 | 预期结果 |
|---|------|------|------|----------|
| 1 | `/api/ai/chat` | POST | `{ message, history }` | 200，返回AI回复 |
| 2 | `/api/ai/generate` | POST | `{ prompt, type }` | 200，返回生成内容 |
| 3 | `/api/ai/write` | POST | `{ content }` | 200，AI写作完成 |

### 4.6 匹配API
| # | 端点 | 方法 | 参数 | 预期结果 |
|---|------|------|------|----------|
| 1 | `/api/match/recommend` | GET | `?page=1&size=10` | 200，返回匹配推荐列表 |
| 2 | `/api/match/{id}` | GET | — | 200，返回匹配详情 |
| 3 | `/api/match/{id}/unlock` | POST | — | 200，解锁匹配成功 |

### 4.7 信任/人脉API
| # | 端点 | 方法 | 参数 | 预期结果 |
|---|------|------|------|----------|
| 1 | `/api/trust/network` | GET | — | 200，返回信任网络数据 |
| 2 | `/api/business-card/six-degrees/{userId}/network` | GET | `?max_depth=3` | 200，返回六度人脉图谱 |

### 4.8 OCR扫描API
| # | 端点 | 方法 | 参数 | 预期结果 |
|---|------|------|------|----------|
| 1 | `/api/ocr/scan` | POST | multipart `file` | 200，返回结构化名片信息 |

### 4.9 消息API
| # | 端点 | 方法 | 参数 | 预期结果 |
|---|------|------|------|----------|
| 1 | `/api/messages` | GET | — | 200，返回会话列表 |
| 2 | `/api/messages/{conversationId}` | GET | `?page=1&page_size=50` | 200，返回消息历史 |
| 3 | `/api/messages/unread/count` | GET | — | 200，返回未读数 |

### 4.10 团队API
| # | 端点 | 方法 | 参数 | 预期结果 |
|---|------|------|------|----------|
| 1 | `/api/teams` | GET | — | 200，返回团队列表 |
| 2 | `/api/teams` | POST | `{ name, description }` | 201，创建团队成功 |
| 3 | `/api/teams/{id}` | PUT | `{ name, ... }` | 200，更新团队成功 |

### 4.11 支付API
| # | 端点 | 方法 | 参数 | 预期结果 |
|---|------|------|------|----------|
| 1 | `/api/payment/products` | GET | — | 200，返回商品/定价列表 |
| 2 | `/api/payment/create` | POST | `{ tier, channel, openid }` | 200/201，创建支付订单 |

### 4.12 标签API
| # | 端点 | 方法 | 参数 | 预期结果 |
|---|------|------|------|----------|
| 1 | `/api/tags/me` | GET | — | 200，返回我的标签列表 |
| 2 | `/api/tags` | GET | — | 200，返回全量标签 |

---

## 五、空壳页面已知状态（已处理）

以下页面已在工业化改造中 **移至 `_archive/` 目录**，不再存在于 `pages/` 下：

| 页面 | 路径 | 处理状态 |
|------|------|----------|
| 注册 | `pages/register/` | ✅ 已移除 |
| 名片列表（空壳） | `pages/brochure/brochure` | ✅ 已移除 |
| 导入数据 | `pages/platform/import/` | ✅ 已移除 |
| 平台消息 | `pages/platform/message/` | ✅ 已移除 |
| 审核 | `pages/platform/review/` | ✅ 已移除 |
| 网络首页（空壳） | `pages/network/network` | ✅ 已移除 |
| 消息首页（空壳） | `pages/message/message` | ✅ 已移除 |
| 匹配推荐（独立） | `pages/match/match` | ✅ 已移除 |
| 团队首页（空壳） | `pages/team/team` | ✅ 已移除 |
| 组织首页（空壳） | `pages/organization/organization` | ✅ 已移除 |
| 邀请 | `pages/invite/` | ✅ 已移除 |
| 渐进搜索 | `pages/progressive_search/` | ✅ 已移除 |
| 增长分析 | `pages/analytics/growth/` | ✅ 已移除 |

> 如需恢复，文件在 `miniapp/_archive/` 目录下。

---

## 六、测试环境与工具

| 项目 | 说明 |
|------|------|
| 测试设备 | 微信开发者工具 Stable（模拟器）+ 真机 iPhone/Android |
| 网络环境 | 开发网络（需 `不校验合法域名`）+ 生产网络（card.liankebao.top） |
| 测试账号 | 微信登录（真实） + mock 登录（开发） |
| 辅助工具 | 微信开发者工具 → Network 面板、Storage 面板、Console |
| API 验证 | `curl -X GET "https://card.liankebao.top/api/brochures" -H "Authorization: Bearer <token>"` |

---

## 七、回归测试指引

每次代码改动后，按以下优先级执行：

1. **P0（必须）**：全局检查（第一章全部）+ 登录流程（第二章全部）
2. **P1（高）**：被改动的页面（第三章对应页面全部测试项）
3. **P2（中）**：被改动页面关联的API链路（第四章对应条目）
4. **P3（低）**：其余页面的冒烟测试

> 通过标准：P0 + P1 100%通过，P2 ≥ 90%通过，P3 无新增阻塞性Bug。
